QUY TẮC ĐẶT TÊN HÀMNguyên tắc đặt tên:Lớp: Danh từ + viết hoa chữ cái đầuĐúng: Room, Customer, BookingOrder,…Sai: room, customer, booking_order,….Hàm: sử dụng động từ + viết thường chữ cái đầu tiên, đặt rõ hành động thực hiện.Đúng: processCheckIn(), processCheckOut(), calculateTotalAmount(),…Sai:  CheckIn(), checkIn(), totalAmount(),…Biến: danh từ + viết thường chữ cái đầuĐúng: roomList, serviceList, totalPrice,…Sai: ListRoom, list, PriceTotal,…..Nguyên tắc thiết kế hàm:1 hàm cần làm duy nhất 1 việc, giả sử nếu việc đó có cả lưu CSDL, gửi email, xác thực,… cần tách riêng ra.Độ dài dòng: không nên vượt quá 20-30 dòng.Không nên viết theo if (status == 2) mà nên viết if (status == BookingStatus.PAID), giải thích PAID lấy từ public enum PAID(1),. Nên dùng Enum hoặc hằng số.Bảng tra cứu thuật ngữ:Chức năng nghiệp vụ Quy ướcTên Hàm, API Endpoint chuẩnTra cứu phòng trốngget, check, findgetAvailableRooms()Đặt phòng trực tuyến hoặc trực tiếpcreate, bookbookRoom(), createBookingOrder()Hủy đặt phòngcancelcancelBooking()Làm thủ tục nhận phòngprocess, handleprocessCheckIn()Làm thủ tục trả phòng process, handleprocessCheckOut()Xác thực CCCD extract, verify, scanextractIdCardData(), verifyIdentity()Dự đoán rủi ro không đến predict, analyzepredictNoShowRisk(), analyzeBookingRisk()Tư vấn tự độnganalyze, replyanalyzeIntent(), generateChatbotResponse()Xử lý thanh toánprocessprocessPayment()Thanh toán tiền mặtprocess, confirmprocessCashPayment()Thanh toán chuyển khoản generate, creategenerateVietQrCode()Cập nhật trạng thái phòngupdate, changeupdateRoomStatus()Lưu ý:Nên chú thích mỗi hàm tiếng Việt khi mang lên bảo vệ hội đồng.